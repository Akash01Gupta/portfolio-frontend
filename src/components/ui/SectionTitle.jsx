export default function SectionTitle({ title }) {
  return (
    <h2 className="text-center text-4xl font-extrabold bg-gradient-to-r from-cyan-400 via-blue-400 to-purple-500 bg-clip-text text-transparent">
      {title}
    </h2>
  );
}
